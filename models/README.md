# AI Models

## Embedding Model

Sentence Transformers

Model:

```text
all-MiniLM-L6-v2

Purpose

The embedding model converts document text into numerical vector representations.

These embeddings allow the system to compare semantic meaning between:

User questions
Document chunks
Future Improvements
OpenAI embeddings
BGE embeddings
Instructor models
Domain-specific embeddings
